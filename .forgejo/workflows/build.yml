name: build-and-push
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  images:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to Forgejo registry
        run: echo "${{ secrets.REGISTRY_TOKEN }}" | docker login ${{ vars.REGISTRY_HOST }} -u ${{ github.repository_owner }} --password-stdin

      - name: Build & push backend image
        run: |
          IMG=${{ vars.REGISTRY_HOST }}/${{ github.repository_owner }}/wardrobe-backend
          docker build -t "$IMG:latest" -t "$IMG:${{ github.sha }}" ./backend
          docker push "$IMG:latest"
          docker push "$IMG:${{ github.sha }}"

      - name: Build & push frontend image
        run: |
          IMG=${{ vars.REGISTRY_HOST }}/${{ github.repository_owner }}/wardrobe-frontend
          docker build --build-arg NEXT_PUBLIC_API_URL="" -t "$IMG:latest" -t "$IMG:${{ github.sha }}" ./frontend
          docker push "$IMG:latest"
          docker push "$IMG:${{ github.sha }}"
